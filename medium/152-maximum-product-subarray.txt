# 152. Maximum Product Subarray
# Difficulty: Medium
# URL: https://leetcode.com/problems/maximum-product-subarray/
# Date: 10/03/2026

# ========== Solution A ==========
# Language: txt
# Date: 10/03/2026

class Solution:class Solution:
    def maxProduct(self, nums: List[int]) -> int:    def maxProduct(self, nums: List[int]) -> int:
        maxP = float("-inf")        maxP = float("-inf")
        positive = negative = nums[0]        positive = negative = nums[0]
        for num in nums[1:]:        for num in nums[1:]:
            if num <0:            if num <0:
                positive,negative = negative,positive                positive,negative = negative,positive
            positive = max(num,num*positive)            positive = max(num,num*positive)

            negative = min(num,num*negative)            negative = min(num,num*negative)
            maxP = max(maxP,positive)            maxP = max(maxP,positive)
        return maxP        return maxP


